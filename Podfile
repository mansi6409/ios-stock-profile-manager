# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Stock Profile Manager' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Stock Profile Manager
  pod 'Alamofire', '~> 5.4'
  pod 'SwiftyJSON', '~> 5.0'
  pod 'Kingfisher', '~> 7.0'

  target 'Stock Profile ManagerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Stock Profile ManagerUITests' do
    # Pods for testing
  end

end
